services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: encore
      POSTGRES_PASSWORD: encore
      POSTGRES_DB: encore
    ports: ["5432:5432"]
    volumes: ["encore-data:/var/lib/postgresql/data"]
volumes:
  encore-data:
